{% extends 'base.html.twig' %}

{% block title %} Detalle de tarea {% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h2>Detalle de tarea: {{ task.title }}</h2>
        <p class="data-task">
            {{ task.createdAt.format('d/m/Y') }}

        </p>
        <p>
            {{ task.content }}
        </p>
    </div>
{% endblock %}